services:
  dev:
    build: .
    ports:
      - "4000:4000"   # site
      - "35729:35729" # livereload
    command: >
      bundle exec jekyll serve --host 0.0.0.0 --livereload
    volumes:
      - .:/srv/jekyll
